sim.enrichment {
  messageTypes = [WORK, LY_MARKER]
  defaultPdf { preset = uniform }
  defaultEdgeLabel = WORK
  perEdgeLabels {
    "0_1" = WORK
    "1_2" = LY_MARKER
  }
}

sim.runtime {
  seed = 42
  algorithms = [ "lai-yang" ]
  algorithmInitiators = [1]
  initiators {
    timers = [
      { node = 0, tickEveryMs = 120, mode = "fixed", fixedMsg = "WORK" }
    ]
  }
}
